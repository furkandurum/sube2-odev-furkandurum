#include<stdio.h>
int main(){
    int n;
    float toplam;
    scanf("%f",&n);
    for(int k=1;k<=n;k++){
        toplam= toplam + ((k*k)+1,23)/(k-0,25);
    }
    printf("%.2f",toplam);

    return 0;
}
